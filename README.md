## Ethereum credit channels

### Running locally

You need to run a local test node (such as [ganache-cli](https://github.com/trufflesuite/ganache-cli)) on port `8545` (default port).

Then just run
```
npm start
```
